# Core backend dependencies — everything except heavy optional engines.
# Used directly by CI (tests) and included from requirements.txt for Docker.
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1   # required for pydantic EmailStr (newsletter subscribe endpoint)
python-jose[cryptography]==3.3.0
bcrypt==4.1.3            # password hashing (used directly via `import bcrypt` in services/auth.py)
httpx==0.27.0
python-multipart==0.0.9
beautifulsoup4==4.12.3
requests==2.32.3
apscheduler==3.10.4
PyYAML==6.0.1            # settings.yaml editable config mirror
