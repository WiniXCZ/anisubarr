-r requirements-core.txt

# Subtitle timing sync fallback engine (alass is the primary engine, installed
# as a native binary in the Docker image — see Dockerfile). Needs build-essential
# to compile webrtcvad, which is why CI installs only requirements-core.txt.
ffsubsync==0.4.25
